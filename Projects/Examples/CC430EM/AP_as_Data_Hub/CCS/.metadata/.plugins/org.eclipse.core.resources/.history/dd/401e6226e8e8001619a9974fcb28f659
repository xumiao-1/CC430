#include <stdbool.h>
#include "bsp.h"
#include "mydef.h"

uint32_t gNextWkup = 0;



/**************************************************
 * PUBLIC FUNCTIONS
 *************************************************/
bool node_init(void)
{
    /* hardware-related initialization */
    BSP_Init();
    rtc_init();
    lpw_init();

    /* software-related initialization */
    task_pool_init();
    soft_initTimers();
    tmpsnr_registerMsgProcessor(); /* register RF msg processors */

    return true;
}


// eof...
